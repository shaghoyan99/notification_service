spring:
  application:
    name: notification_service
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres?currentSchema=notification_service
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        default_schema: notification_service
  liquibase:
    enabled: true
    change-log: classpath:db/master.xml
  mail:
    host: smtp.gmail.com
    port: 587
    username: wahejavawahe@gmail.com
    password: u o d e p j n b y j s g h r j q
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true